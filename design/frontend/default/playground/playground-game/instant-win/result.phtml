<?php
$this->plugin('translate')->setTranslatorTextDomain('playgroundgame');
$playLimitScale = $game->getPlayLimitScale();
$playLimitValue = '';
if ($playLimitScale == 'day') : $playLimitValue = 'dès demain';
elseif ($playLimitScale == 'week') : $playLimitValue = 'dès la semaine prochaine';
elseif ($playLimitScale == 'month') : $playLimitValue = 'dès le mois prochain';
elseif ($playLimitScale == 'year') : $playLimitValue = 'dès l\'année prochaine';
else : $playLimitValue = 'à un autre moment';
endif;
?>
<div class="game-instantwin row result">
	<h1>
	    <?php echo $this->game->getTitle(); ?>
	</h1>
	<?php if(count($this->flashMessages)>0):?>
		<div class="row lose">
			<div class="col-md-2">
				<img src="<?php echo $this->frontendAssetPath('images/common/round-wrong.png')?>" alt="" />
			</div>
			<div class="col-md-10">
				<p id="valid-text">
				    <?php echo $this->flashMessages[0];?>
				</p>
			</div>
		</div>
    <?php else:?>
    	<?php if($this->winner):?>
    		<div class="row win">
    			<div class="col-md-1">
    				<img src="<?php echo $this->frontendAssetPath('images/common/round-valid.png')?>" alt="" />
    			</div>
    			<div class="col-md-11">
    				<p>
    				    Félicitations, vous avez gagné !
    				</p>
    			</div>
    		</div>
    	<?php else:?>
    		<div class="row lose">
    			<div class="col-md-1">
    				<img src="<?php echo $this->frontendAssetPath('images/common/round-wrong.png')?>" alt="" />
    			</div>
    			<div class="col-md-11">
    				<p>
    				    Retentez votre chance <?php echo $playLimitValue; ?>!
    				</p>
    			</div>
    		</div>
    	<?php endif;?>
	<?php endif;?>
	<div id="share-instantwin">
	    <?php if($this->winner):?>
            <h2>
                <span>
                    Partagez le jeu...
                </span>
            </h2>
		<?php else:?>
            <h2>
                Partagez le jeu...
                <br/>
                <span>
                    et gagnez 250 points par ami parrainé
                </span>
            </h2>
		<?php endif;?>
		<!-- SPONSOR (parrainer) -->
        <div id="parrainer">
			<?php echo $this->partial('playground-game/partial/result-share', array(
		            'game'			=> $this->game,
		            'secretKey'		=> $this->secretKey,
		            'socialLinkUrl' => $socialLinkUrl,
		            'form'			=> $this->form,
	            	'statusMail'    => $statusMail,
		            'gameType'		=> 'instantwin', 
					'channel' => $this->channel
		    )); ?>
		</div>
		<!-- //END SPONSOR (parrainer) -->
	</div>
</div>